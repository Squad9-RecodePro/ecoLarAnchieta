import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
        marginTop: 300,
    },
    blocovolun: {
        width: '80%',
        height: '10%',
    },
    titulovolun: {
        fontSize: 20,
    }
})

export default styles;